{{#withPlatformChannelPluginHook}}
// swift-tools-version: 5.9
// The swift-tools-version declares the minimum version of Swift required to build this package.

import PackageDescription

let package = Package(
    name: "FlutterTestPackage",
    platforms: [
        .macOS("10.14")
    ],
    products: [
        .library(name: "FlutterTestPackage", targets: ["FlutterPluginTests"])
    ],
    dependencies: [
        .package(path: "../FlutterPackage"),
        .package(name: "{{pluginProjectName}}", path: "../../../../../macos/{{pluginProjectName}}")
    ],
    targets: [
        .testTarget(
            name: "FlutterPluginTests",
            dependencies: [
                "FlutterPackage",
                .product(name: "{{pluginProjectName}}_test", package: "{{pluginProjectName}}")
            ]
        )
    ]
)
{{/withPlatformChannelPluginHook}}