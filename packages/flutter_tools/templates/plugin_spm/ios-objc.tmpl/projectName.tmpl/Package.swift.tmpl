// swift-tools-version: 5.9
// The swift-tools-version declares the minimum version of Swift required to build this package.

import PackageDescription

let package = Package(
    name: "{{projectName}}",
    platforms: [
        .iOS("12.0"),
    ],
    products: [
        .library(name: "{{projectName}}", targets: ["{{projectName}}"]),
        .library(name: "{{projectName}}_test", targets: ["{{projectName}}_test"]),
    ],
    dependencies: [
    ],
    targets: [
        .target(
            name: "{{projectName}}",
            dependencies: [],
            resources: [
               .process("Resources"),
            ],
            cSettings: [
                .headerSearchPath("include/{{projectName}}"),
            ]
        ),
        // TODO: SPM comment
        // Exposes unit tests to the example application
        .target(
            name: "{{projectName}}_test",
            dependencies: [
                "{{projectName}}"
            ],
            resources: [
               .process("Resources"),
            ],
            cSettings: [
                .headerSearchPath("../{{projectName}}"),
            ]
        ),
    ]
)
